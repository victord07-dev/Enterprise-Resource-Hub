All 7 fixes approved.

Two corrections before implementation:

CORRECTION 1 — Column naming for K9-6:
Don't use 'supplier_entered_total'. Mirror the Phase 1 sales-side
T93 naming convention (ext_invoice_number, ext_invoice_date,
ext_total_amount, ext_gst_amount on sales_invoices).

For supplier_invoices, add:
- ext_total_amount (numeric, nullable) — what the supplier's paper
  invoice actually states
- ext_gst_amount (numeric, nullable, optional) — matches sales-side
  pattern, allows accountant to record tax breakdown if useful

Existing supplier_invoices.total_amount stays as the system-computed
value from GRN confirm (the F1 auto-create result).

Variance check: compare ext_total_amount (entered by user) vs
total_amount (system value). Threshold ₹5.

CORRECTION 2 — Existing field clarity:
Confirm the existing supplier_invoices schema has:
- invoice_number (currently set to "SI-2026-XXXX" auto-generated by F1)
- supplier_invoice_number (or similar — for external paper reference?)

If "invoice_number" is the auto-generated system reference and a
separate field for supplier's paper number doesn't exist:
- Add ext_invoice_number column (text, nullable)
- The Mark as Recorded form's "Supplier Invoice Number" field stores
  here, NOT in the auto-generated invoice_number

If they share one field today: that's a bug — accountant overwriting
the auto-generated system reference with the supplier's paper number
loses the system's traceability. Need separate columns.

Verify schema first, report what currently exists, fix accordingly.

After verification, implement all 7 fixes:

K9-1: Add "Ext. Total Amount (₹)" required field + asterisk + validation
K9-2: Add signed copy file upload widget (PDF/JPEG/PNG, max 10MB)
      Stores ext_signed_copy_url, ext_signed_copy_uploaded_by,
      ext_signed_copy_uploaded_at on the row
K9-3: Mark "Ext. Invoice Date" required + asterisk + validation
K9-4: Disable Mark as Recorded button until all 3 required fields
      filled (number, date, total) + signed copy uploaded
K9-5: Variance modal:
      - Triggers when |ext_total_amount - total_amount| > ₹5
      - Body shows: external total | system total | difference |
        continue/cancel
      - Within ₹5: silent record
      - Mirror exact pattern of sales-side T93 variance modal
K9-6: Add ext_total_amount + ext_gst_amount + ext_invoice_number +
      ext_invoice_date columns. Run db:push. Backfill nothing
      (these are for future-going-forward records).
K9-7: Add logAction(req.user.id, 'supplier_invoice_recorded',
      'accounts', `Supplier invoice ${invoice_number} recorded.
      Ext invoice: ${ext_invoice_number} dated ${ext_invoice_date}
      for ₹${ext_total_amount}.${variance ? ' Variance from system:
      ₹' + variance : ''}`) call in PATCH route after successful
      mark-recorded

Also: the optional Ext. GST Amount field per sales-side pattern
should be in the form (clearly labeled optional, no asterisk).

After ship, walk K9 items end-to-end:

(pp) Open SI-2026-0002 (or any pending_upload supplier invoice).
     Form shows 4 fields: Number*, Date*, Total*, GST Amount(optional).
     Plus signed copy upload section. Plus disabled Mark as Recorded
     button. Screenshot.

(qq) Try Mark as Recorded with Total empty → button disabled OR
     submission blocked. Screenshot.

(rr) Fill all 3 required + signed copy. Total exceeds variance by
     ₹10,000 → variance modal appears. Continue → recorded.
     Screenshot modal.

(ss) Use another pending invoice. Total within ₹5 → no modal,
     silent record. SQL verify ext_* fields populated, status
     'recorded', upload_status 'recorded'.

(tt) SQL: SELECT user_id, action, details FROM audit_logs WHERE
     action='supplier_invoice_recorded' ORDER BY timestamp DESC
     LIMIT 5; Confirm entries with full details.

After K9 closes, move to K10 (PO GST capture, 5 items).