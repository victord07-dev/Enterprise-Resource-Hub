Phase 6.6 — CSV re-import UPDATE mode + fuzzy duplicates + PO unit-cost source + Task #75 closure + Pricing page fix
Four sections. Commit and push after each. Zero changes to Daily Pricing engine, computeFifoLots, GRN confirmation, sales totals math, audit trail, or auth — additions only.
Process rule (standing): before declaring any section complete, walk through every affected user-facing path in Build mode and show me screenshots/DB evidence. Backend test scripts do not count as verification. Only I (operator) declare phase closure.
Section A — CSV re-import UPDATE mode
A1. Extend POST /api/products/import: existing SKUs now UPDATE instead of skip. Dry-run response adds new field would_update: { row_number, sku, changes: [{ field, old_value, new_value }] }[].
Updatable fields: distributorPrice, unitPrice, mrp, specs, warrantyPeriod, applicableRegions, almm, dcrCompliant, modelSeries, lifecycleStatus, priceListVersion, packSize, logisticsCost, minMarginPct, targetMarginPct, customerTierPrice, supplierSku.
Immutable fields (if CSV row differs, emit warning in dry-run, skip THAT FIELD only, don't skip the row): sku, name, brand, category, hsnCode, gstRate.
When almm or dcrCompliant flips, surface in a dedicated "Flag Changes" highlight panel ABOVE the general Changes preview — regulatory flags need extra visibility.
A2. Dry-run UI: add 4th stat card "Would Update" (blue) alongside Would Import / Would Skip / Errors. Below stats, collapsible "Changes preview" with compact diff table (row_number, SKU, field, old → new).
A3. Commit mode: apply updates in same transaction as inserts. Response: add updated: number to ImportCommitResult.
Section B — Fuzzy duplicate detection
B1. During dry-run, for NEW-SKU rows, flag possible duplicates against existing products when all three conditions match: (same brandId OR same brand name case-insensitive) AND (same category) AND (normalized name similarity ≥ 85%). Normalize: lowercase, strip punctuation, collapse whitespace; preserve semantic tokens (kw, kwh, ah, w, v).
B2. Use Levenshtein ratio or equivalent. Library choice yours.
B3. Dry-run response: possible_duplicates: { row_number, incoming_sku, incoming_name, existing_sku, existing_name, similarity_pct }[]. UI: yellow warning block above Changes preview. Operator-driven; no auto-merge. Commit proceeds normally unless operator aborts.
Section C — PO supplier price sync + Task #75 closure + Pricing page fix
C1. Add column supplier_products.lastPriceUpdatedAt TIMESTAMP NULL (additive, non-breaking). Run npm run db:push.
C2. PO creation form: each line item's "Unit Cost" field defaults from supplier_products.supplierPrice for the (selected supplier, selected product) pair. Currently pulls from products.costPrice. Fix. Fallback if no supplier_products link exists (defensive, shouldn't trigger post-backfill): products.distributorPrice. Field remains editable.
C3. On PO save (draft OR issue): if entered unit cost differs from current supplier_products.supplierPrice for that supplier+product pair:

UPDATE supplier_products.supplierPrice to new value
SET supplier_products.lastPriceUpdatedAt = NOW()
SET the product's needsPricingReview = true
Write audit entry { action: 'supplier_price_updated', productId, supplierId, oldPrice, newPrice, poId, updatedBy }
Do NOT update products.distributorPrice — that stays the brand's published list price

C4. SupplierProductCatalog display (SupplyChain.tsx ~line 374): add "(updated X days ago)" hint next to supplier price when lastPriceUpdatedAt is populated.
C5. Task #75 closure — find the sales-order-driven auto-purchase-request generator (checkAndCreatePurchaseRequests in server/routes.ts). Currently reads unit cost from products.costPrice. Fix to prefer supplier_products.supplierPrice for the primary supplier (isPrimary = true) of each product. Fallback to products.costPrice. Fallback to products.distributorPrice.
C6. Pricing page fix — Pricing.tsx line 351 currently reads supplierPrice = product.costPrice ? Number(product.costPrice) : null. Change to fetch primary supplier's price. Add/extend endpoint to return primarySupplierPrice per product (prefer isPrimary = true, else minimum supplierPrice among links). Update the "Supplier Price" column display. The existing ShieldAlert icon on Pricing.tsx line 374 and product.needsPricingReview flag stay — C3 now actually triggers the flag.
C7. Add code comment at top of the pricing engine module:
/**
 * Cost source chain (reference only):
 * products.distributorPrice → supplier_products.supplierPrice (auto-linked on product create
 *                              or CSV import via Phase 6.5 backfill)
 * → po_items.unitCost (defaults from supplier_products.supplierPrice of primary link;
 *                      editable; saving a PO with a different cost UPDATES supplier_products
 *                      and sets needsPricingReview)
 * → grn_items.unitCost (locks on GRN confirm)
 * → FIFO lot consumption (this engine).
 *
 * Supplier price updates affect FUTURE POs/GRNs only. Existing lots preserve locked costs.
 * Phase 6.6 does not change engine behavior — only upstream plumbing.
 */
Section D — Pre-stop-gate integrity check
D1. Before declaring Phase 6.6 done, record blendedCost for 3 pre-deploy products with confirmed GRN lots. Deploy Phase 6.6. Re-open same 3 products' Daily Pricing. blendedCost must match exactly. If not, engine was disturbed — roll back and investigate.
Stop gate verification (operator-driven, I'll walk these)
(a) Create test CSV with 2 rows that change distributorPrice on 2 existing Eastman products. Dry-run shows "Would Update: 2" with diff. Commit → products now show new prices.
(b) Test CSV with one row's name changed on an existing SKU. Dry-run shows immutable-field warning "name is immutable; field skipped". Other fields still update on commit.
(c) Test CSV flipping almm: true → false on a panel. Dry-run shows flip in the dedicated "Flag Changes" panel above the general preview.
(d) New-SKU CSV row with ≥85% similarity to existing product in same brand+category. Dry-run shows yellow Possible Duplicates block with match, similarity %, both SKUs side-by-side.
(e) Raise test PO for an Eastman product. Line's Unit Cost defaults from supplier_products.supplierPrice, NOT from products.costPrice. Note the value.
(f) Edit PO unit cost to a new value. Save/issue. Go to SupplyChain → Eastman → Product Catalog → that product. supplierPrice now shows new value + "(updated just now)" hint.
(g) Check audit trail — one supplier_price_updated entry with old/new values + poId.
(h) Open Pricing page for that product. "Supplier Price" column shows NEW value (from supplier_products). ShieldAlert icon appears next to product name (needsPricingReview now true).
(i) Create sales order with bundle containing short-stock products. Trigger auto-PR. Confirm generated PR line items' unit_cost comes from supplier_products.supplierPrice (primary) → NOT NULL as long as primary links exist (verified by Phase 7 closure).
(j) Regression: Daily Pricing blendedCost for the 3 sample products from D1 — unchanged.